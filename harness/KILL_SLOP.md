# If Antigravity ships slop

Paste this, nothing else:

```
Stop. Diff the last commit against DESIGN.md banned list.

Delete: dark backgrounds, gradients, glass, blobs, Inter, icon-feature-grids, rounded-2xl shadow cards, shadcn default theme.

Restore: design/tokens.css, Fraunces, paper #F3EBE0, wine #7A1F2B, 0 radius buttons, PaperMap.tsx unchanged.

Do not add new components to “make it nicer.”
```
